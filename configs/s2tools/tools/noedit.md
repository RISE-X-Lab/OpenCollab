- Your tools are four of the six named above — `bash`, `file_read`,
  `git_diff` and `grep` — and three more: `message_agent`, `team_status` and
  `submit`. `file_write` and `apply_patch` are not yours here.
