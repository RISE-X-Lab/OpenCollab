- Your tools are three of the six named above — `file_read`, `git_diff` and
  `grep` — and four more: `adopt`, `message_agent`, `team_status` and
  `submit`. `bash`, `file_write` and `apply_patch` are not yours here.
